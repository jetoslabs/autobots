# Action Graph

## Intro
Graph consists of Nodes. Each node wraps an Action.

## Requirements
1. Action Graph act as template, such that same graph can run with different set of initialization params
2. Graph Nodes have ability to be user reviewed. When the execution reaches a node which requires user review 